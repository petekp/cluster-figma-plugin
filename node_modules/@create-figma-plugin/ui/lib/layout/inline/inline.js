import { h, toChildArray } from 'preact';
import styles from './inline.module.css';
export function Inline({ children, space, ...rest }) {
    return (h("div", { ...rest, class: typeof space === "undefined" ? undefined : styles[space] }, toChildArray(children).map(function (child, index) {
        return (h("div", { key: index, class: styles.child }, child));
    })));
}
//# sourceMappingURL=inline.js.map