import { ComponentChildren, JSX } from 'preact';
import { Props } from '../../types/types';
export type ButtonProps = {
    children: ComponentChildren;
    danger?: boolean;
    disabled?: boolean;
    fullWidth?: boolean;
    loading?: boolean;
    onClick?: JSX.MouseEventHandler<HTMLButtonElement>;
    propagateEscapeKeyDown?: boolean;
    secondary?: boolean;
};
export declare function Button({ children, danger, disabled, fullWidth, loading, onClick, propagateEscapeKeyDown, secondary, ...rest }: Props<HTMLButtonElement, ButtonProps>): JSX.Element;
//# sourceMappingURL=button.d.ts.map