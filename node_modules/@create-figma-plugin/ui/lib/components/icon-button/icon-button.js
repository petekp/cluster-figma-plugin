import { h } from 'preact';
import { useCallback } from 'preact/hooks';
import styles from './icon-button.module.css';
export function IconButton({ children, disabled = false, onClick, propagateEscapeKeyDown = true, ...rest }) {
    const handleKeyDown = useCallback(function (event) {
        if (event.key !== "Escape") {
            return;
        }
        if (propagateEscapeKeyDown === false) {
            event.stopPropagation();
        }
        event.currentTarget.blur();
    }, [propagateEscapeKeyDown]);
    return (h("button", { ...rest, class: styles.iconButton, disabled: disabled === true, onClick: disabled === true ? undefined : onClick, onKeyDown: disabled === true ? undefined : handleKeyDown, tabIndex: disabled === true ? -1 : 0 }, h("div", { class: styles.icon }, children)));
}
//# sourceMappingURL=icon-button.js.map