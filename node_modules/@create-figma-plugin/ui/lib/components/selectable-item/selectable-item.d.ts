import { ComponentChildren, JSX } from 'preact';
import { OnValueChange, Props } from '../../types/types';
export type SelectableItemProps<Name extends string> = {
    bold?: boolean;
    children: ComponentChildren;
    disabled?: boolean;
    indent?: boolean;
    name?: Name;
    onChange?: OmitThisParameter<JSX.GenericEventHandler<HTMLInputElement>>;
    onValueChange?: OnValueChange<boolean, Name>;
    propagateEscapeKeyDown?: boolean;
    value: boolean;
};
export declare function SelectableItem<Name extends string>({ bold, children, disabled, indent, name, onChange, onValueChange, propagateEscapeKeyDown, value, ...rest }: Props<HTMLInputElement, SelectableItemProps<Name>>): JSX.Element;
//# sourceMappingURL=selectable-item.d.ts.map