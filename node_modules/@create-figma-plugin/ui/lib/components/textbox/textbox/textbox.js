import { h } from 'preact';
import { createClassName } from '../../../utilities/create-class-name';
import { RawTextbox } from './private/raw-textbox';
import styles from './textbox.module.css';
export function Textbox({ icon, variant, ...rest }) {
    if (typeof icon === "string" && icon.length !== 1) {
        throw new Error(`String \`icon\` must be a single character: ${icon}`);
    }
    return (h("div", { class: createClassName([
            styles.textbox,
            typeof variant === "undefined"
                ? null
                : variant === "border"
                    ? styles.hasBorder
                    : null,
            typeof icon === "undefined" ? null : styles.hasIcon,
            rest.disabled === true ? styles.disabled : null
        ]) }, h(RawTextbox, { ...rest, class: styles.input }), typeof icon === "undefined" ? null : (h("div", { class: styles.icon }, icon)), h("div", { class: styles.border }), variant === "underline" ? h("div", { class: styles.underline }) : null));
}
//# sourceMappingURL=textbox.js.map