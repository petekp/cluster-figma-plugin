import { h } from 'preact';
import { useCallback } from 'preact/hooks';
import { createClassName } from '../../utilities/create-class-name';
import styles from './toggle.module.css';
export function Toggle({ children, disabled = false, name, onChange = function () { }, onValueChange = function () { }, propagateEscapeKeyDown = true, value = false, ...rest }) {
    const handleChange = useCallback(function (event) {
        const newValue = event.currentTarget.checked;
        onValueChange(newValue, name);
        onChange(event);
    }, [name, onChange, onValueChange]);
    const handleKeyDown = useCallback(function (event) {
        if (event.key !== "Escape") {
            return;
        }
        if (propagateEscapeKeyDown === false) {
            event.stopPropagation();
        }
        event.currentTarget.blur();
    }, [propagateEscapeKeyDown]);
    return (h("label", { class: createClassName([
            styles.toggle,
            disabled === true ? styles.disabled : null
        ]) }, h("input", { ...rest, checked: value === true, class: styles.input, disabled: disabled === true, name: name, onChange: handleChange, onKeyDown: handleKeyDown, tabIndex: disabled === true ? -1 : 0, type: "checkbox" }), h("div", { class: styles.box }), h("div", { class: styles.switch }), h("div", { class: styles.children }, children)));
}
//# sourceMappingURL=toggle.js.map