import { ComponentChildren, JSX } from 'preact';
import { OnValueChange, Props } from '../../types/types';
export type DropdownProps<Name extends string, Value extends boolean | number | string = string> = {
    disabled?: boolean;
    icon?: ComponentChildren;
    name?: Name;
    onChange?: OmitThisParameter<JSX.GenericEventHandler<HTMLInputElement>>;
    onValueChange?: OnValueChange<Value, Name>;
    options: Array<DropdownOption<Value>>;
    placeholder?: string;
    value: null | Value;
    variant?: DropdownVariant;
};
export type DropdownOption<Value extends boolean | number | string = string> = DropdownOptionHeader | DropdownOptionValue<Value> | DropdownOptionSeparator;
export type DropdownOptionHeader = {
    header: string;
};
export type DropdownOptionValue<Value> = {
    disabled?: boolean;
    text?: string;
    value: Value;
};
export type DropdownOptionSeparator = {
    separator: true;
};
export type DropdownVariant = 'border' | 'underline';
export declare function Dropdown<Name extends string, Value extends boolean | number | string = string>({ disabled, icon, name, options, onChange, onValueChange, placeholder, value, variant, ...rest }: Props<HTMLDivElement, DropdownProps<Name, Value>>): JSX.Element;
//# sourceMappingURL=dropdown.d.ts.map