import { ComponentChildren, JSX } from 'preact';
import { OnValueChange, Props } from '../../types/types';
export type LayerProps<Name extends string> = {
    bold?: boolean;
    children: ComponentChildren;
    component?: boolean;
    description?: string;
    icon: ComponentChildren;
    name?: Name;
    onChange?: OmitThisParameter<JSX.GenericEventHandler<HTMLInputElement>>;
    onValueChange?: OnValueChange<boolean, Name>;
    propagateEscapeKeyDown?: boolean;
    value: boolean;
};
export declare function Layer<Name extends string>({ bold, children, description, component, icon, name, onChange, onValueChange, propagateEscapeKeyDown, value, ...rest }: Props<HTMLInputElement, LayerProps<Name>>): JSX.Element;
//# sourceMappingURL=layer.d.ts.map