import { h } from 'preact';
import { useState } from 'preact/hooks';
import { useInitialFocus } from '../../../hooks/use-initial-focus/use-initial-focus';
import { IconLayerComponent16 } from '../../../icons/icon-16/icon-layer-component-16';
import { Layer } from '../layer';
export default {
    parameters: {
        fixedWidth: true,
        order: 4
    },
    title: "Components/Layer/Component Selected"
};
export const Passive = function () {
    const [value, setValue] = useState(true);
    function handleChange(event) {
        const newValue = event.currentTarget.checked;
        console.log(newValue);
        setValue(newValue);
    }
    return (h(Layer, { component: true, icon: h(IconLayerComponent16, null), onChange: handleChange, value: value }, "Text"));
};
export const Focused = function () {
    const [value, setValue] = useState(true);
    function handleChange(event) {
        const newValue = event.currentTarget.checked;
        console.log(newValue);
        setValue(newValue);
    }
    return (h(Layer, { ...useInitialFocus(), component: true, icon: h(IconLayerComponent16, null), onChange: handleChange, value: value }, "Text"));
};
export const Bold = function () {
    const [value, setValue] = useState(true);
    function handleChange(event) {
        const newValue = event.currentTarget.checked;
        console.log(newValue);
        setValue(newValue);
    }
    return (h(Layer, { bold: true, component: true, icon: h(IconLayerComponent16, null), onChange: handleChange, value: value }, "Text"));
};
export const Description = function () {
    const [value, setValue] = useState(true);
    function handleChange(event) {
        const newValue = event.currentTarget.checked;
        console.log(newValue);
        setValue(newValue);
    }
    return (h(Layer, { component: true, description: "Description", icon: h(IconLayerComponent16, null), onChange: handleChange, value: value }, "Text"));
};
export const LongText = function () {
    const longText = Array(100).fill("Text").join(" ");
    const [value, setValue] = useState(true);
    function handleChange(event) {
        const newValue = event.currentTarget.checked;
        console.log(newValue);
        setValue(newValue);
    }
    return (h(Layer, { component: true, icon: h(IconLayerComponent16, null), onChange: handleChange, value: value }, longText));
};
export const LongTextDescription = function () {
    const longText = Array(100).fill("Text").join(" ");
    const description = Array(100).fill("Text").join(" ");
    const [value, setValue] = useState(true);
    function handleChange(event) {
        const newValue = event.currentTarget.checked;
        console.log(newValue);
        setValue(newValue);
    }
    return (h(Layer, { component: true, description: description, icon: h(IconLayerComponent16, null), onChange: handleChange, value: value }, longText));
};
export const OnValueChange = function () {
    const longText = Array(100).fill("Text").join(" ");
    const [value, setValue] = useState(true);
    function handleValueChange(newValue) {
        console.log(newValue);
        setValue(newValue);
    }
    return (h(Layer, { component: true, icon: h(IconLayerComponent16, null), onValueChange: handleValueChange, value: value }, longText));
};
//# sourceMappingURL=layer-component-selected.stories.js.map