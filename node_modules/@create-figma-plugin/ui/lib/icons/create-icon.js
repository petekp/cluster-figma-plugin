import { h } from 'preact';
import styles from './icon.module.css';
export function createIcon(path, options) {
    const { width, height } = options;
    return function Icon({ color, ...rest }) {
        return (h("svg", { ...rest, class: styles.icon, height: height, style: typeof color === "undefined"
                ? undefined
                : {
                    fill: `var(--figma-color-icon-${color})`
                }, width: width, xmlns: "http://www.w3.org/2000/svg" }, h("path", { "clip-rule": "evenodd", d: path, "fill-rule": "evenodd" })));
    };
}
//# sourceMappingURL=create-icon.js.map