import { h } from 'preact';
import { createClassName } from '../../utilities/create-class-name';
import styles from './link.module.css';
export function Link({ children, fullWidth = false, href, target, ...rest }) {
    return (h("a", { ...rest, class: createClassName([
            styles.link,
            fullWidth === true ? styles.fullWidth : null
        ]), href: href, target: typeof target === "undefined" ? undefined : target }, children));
}
//# sourceMappingURL=link.js.map