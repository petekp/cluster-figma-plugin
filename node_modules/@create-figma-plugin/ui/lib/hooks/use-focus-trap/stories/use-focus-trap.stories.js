import { Fragment, h } from 'preact';
import { useState } from 'preact/hooks';
import { Button } from '../../../components/button/button';
import { Textbox } from '../../../components/textbox/textbox/textbox';
import { VerticalSpace } from '../../../layout/vertical-space/vertical-space';
import { useFocusTrap } from '../use-focus-trap';
export default { title: "Hooks/Use Focus Trap" };
export const UseFocusTrap = function () {
    const [value, setValue] = useState("Text");
    useFocusTrap();
    function handleClick(event) {
        console.log(event);
    }
    return (h(Fragment, null, h(Textbox, { name: "text", onValueInput: setValue, value: value, variant: "border" }), h(VerticalSpace, { space: "small" }), h(Button, { fullWidth: true, onClick: handleClick }, "Submit")));
};
//# sourceMappingURL=use-focus-trap.stories.js.map